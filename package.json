{
  "name": "@scola/auth-common",
  "version": "3.0.1",
  "description": "Authorization (common)",
  "repository": "github:scola84/node-auth-common",
  "author": "M.C. van den Boogaart <scola@kvdb.name>",
  "license": "MIT",
  "main": "./dist/auth-common.js",
  "jsnext:main": "index.js",
  "dependencies": {
    "@scola/core": "^1.0.2",
    "@scola/validator": "^5.2.2"
  },
  "devDependencies": {
    "rollup": "^0.37.0",
    "rollup-plugin-buble": "^0.15.0",
    "uglify-js": "^2.7.5"
  },
  "scripts": {
    "build": "npm run cleanup && npm run rollup",
    "build:min": "npm run build && npm run min",
    "cleanup": "rm -rf dist",
    "min": "uglifyjs dist/auth-common.js -m -o dist/auth-common.min.js",
    "prepublish": "npm run build:min",
    "rollup": "rollup -c"
  }
}
